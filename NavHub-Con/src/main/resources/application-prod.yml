spring:
  datasource:
    url: jdbc:mysql://your-prod-host:3306/navhub?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    com.navhub: info

app:
  cors:
    allowed-origins: https://your-frontend-domain.com
