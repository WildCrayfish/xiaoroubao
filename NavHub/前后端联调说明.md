# 前后端联调说明

## ✅ 已完成的配置

### 1. 前端 API 配置
- ✅ 已关闭 Mock 模式（`USE_MOCK = false`）
- ✅ API 基础地址：`http://localhost:8080/api`
- ✅ 已配置 Token 自动携带
- ✅ 已配置错误处理和自动跳转

### 2. 后端配置
- ✅ 数据库连接已配置（密码：123456）
- ✅ JWT 密钥已配置（64 字符）
- ✅ 跨域已配置
- ✅ 统一响应格式已配置

## 🚀 启动步骤

### 1. 启动后端服务

```bash
cd NavHub-Con
mvn spring-boot:run
```

**验证后端启动成功：**
- 看到日志：`Tomcat started on port(s): 8080`
- 看到日志：`HikariPool-1 - Start completed.`

### 2. 启动前端服务

```bash
cd NavHub
npm run dev
```

**验证前端启动成功：**
- 浏览器自动打开：`http://localhost:3000`
- 或手动访问该地址

## 🔐 测试登录

### 默认账号
- **用户名：** `admin`
- **密码：** `admin123`

### 测试步骤

1. 访问登录页面：`http://localhost:3000/login`
2. 输入用户名和密码
3. 点击登录
4. 登录成功后自动跳转到首页
5. 右上角显示用户名，表示登录成功

## 🔍 调试技巧

### 1. 查看网络请求

打开浏览器开发者工具（F12）：
- **Network** 标签：查看所有 API 请求
- **Console** 标签：查看错误信息

### 2. 常见问题排查

#### 问题 1：登录失败，提示网络错误

**检查：**
- [ ] 后端服务是否启动（`http://localhost:8080`）
- [ ] 浏览器控制台是否有 CORS 错误
- [ ] 后端日志是否有错误信息

**解决：**
```bash
# 检查后端是否运行
curl http://localhost:8080/api/auth/login
```

#### 问题 2：登录失败，提示用户名或密码错误

**检查：**
- [ ] 用户名是否正确：`admin`
- [ ] 密码是否正确：`admin123`
- [ ] 数据库中是否有该用户

**解决：**
```sql
-- 检查数据库中的用户
SELECT * FROM navhub.user WHERE username = 'admin';
```

#### 问题 3：登录成功但无法获取用户信息

**检查：**
- [ ] Token 是否正确保存到 localStorage
- [ ] 请求头中是否携带了 Token
- [ ] Token 是否过期

**解决：**
```javascript
// 在浏览器控制台检查
console.log(localStorage.getItem('navhub-token'));
console.log(localStorage.getItem('navhub-user'));
```

#### 问题 4：CORS 跨域错误

**检查：**
- [ ] 后端 `CorsConfig.java` 是否正确配置
- [ ] 前端请求的 URL 是否正确

**解决：**
确保后端 `CorsConfig.java` 中允许了前端域名。

## 📋 功能测试清单

### 登录功能
- [ ] 输入正确用户名密码，登录成功
- [ ] 输入错误用户名密码，显示错误提示
- [ ] 登录成功后跳转到首页
- [ ] 登录后右上角显示用户名
- [ ] 登录后可以访问需要登录的页面

### 注销功能
- [ ] 点击注销按钮，清除登录状态
- [ ] 注销后跳转到登录页
- [ ] 注销后无法访问需要登录的页面

### Token 管理
- [ ] Token 正确保存到 localStorage
- [ ] Token 自动携带在请求头中
- [ ] Token 过期后自动跳转登录页
- [ ] 刷新页面后保持登录状态

## 🔧 环境变量配置（可选）

如果需要修改 API 地址，创建 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:8080/api
```

## 📝 API 接口说明

### 登录接口
- **URL:** `POST /api/auth/login`
- **请求体:**
  ```json
  {
    "username": "admin",
    "password": "admin123"
  }
  ```
- **响应:**
  ```json
  {
    "code": 0,
    "message": "ok",
    "data": {
      "token": "eyJhbGciOiJIUzUxMiJ9...",
      "user": {
        "id": "1",
        "username": "admin",
        "isAdmin": true
      }
    }
  }
  ```

### 获取用户信息接口
- **URL:** `GET /api/auth/me`
- **请求头:** `Authorization: Bearer <token>`
- **响应:**
  ```json
  {
    "code": 0,
    "message": "ok",
    "data": {
      "id": "1",
      "username": "admin",
      "isAdmin": true
    }
  }
  ```

### 注销接口
- **URL:** `POST /api/auth/logout`
- **请求头:** `Authorization: Bearer <token>`
- **响应:**
  ```json
  {
    "code": 0,
    "message": "ok",
    "data": null
  }
  ```

## 🎯 下一步

登录功能联调成功后，可以继续：
1. 测试工具列表接口
2. 测试分类接口
3. 测试收藏功能
4. 测试管理员功能

## 💡 提示

- 开发时保持浏览器开发者工具打开，方便查看请求和错误
- 后端日志会显示所有 API 请求，便于调试
- 如果修改了后端代码，需要重启后端服务
- 如果修改了前端代码，Vite 会自动热更新
